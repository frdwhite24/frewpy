pages:
  stage: deploy
  tags:
    - frewpy
  script:
    - echo makewebsite
  artifacts:
    paths:
      - docs/build/html
  only:
    - develop

test:pylint:
  image: python:3.6
  tags:
    - frewpy
  script:
    - pip install pylint --quiet
    - pylint --ignored-classes=_socketobject frewpy

test:pytest:
  image: python:3.6
  tags:
    - frewpy
  script:
    - pip install pytest --quiet
    - pytest
